{
  "name": "mqsf-2026-program",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node cloudflare/check.mjs && node backend/check-calendar.mjs",
    "dev:api": "wrangler dev --config cloudflare/wrangler.jsonc",
    "deploy:api": "wrangler deploy --config cloudflare/wrangler.jsonc"
  },
  "devDependencies": { "wrangler": "4.140.0" }
}
